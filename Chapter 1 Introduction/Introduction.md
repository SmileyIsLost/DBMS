# Introduction
SQL (Structured Query Language) is the standard language used to work with relational databases. It lets you store data (tables), retrieve and filter it (SELECT with conditions), combine data from multiple tables (JOIN), and change data (INSERT, UPDATE, DELETE). It’s also used to manage database structure and permissions (like creating tables and controlling access).
